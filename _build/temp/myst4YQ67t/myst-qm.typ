/* Written by MyST v1.9.0 */

#import "myst-imports.typ": *

=== Essential quantum mechanics for this course <essential-quantum-mechanics-for-this-course>

+ Single particle:

  + Phase: - global ($e^(i phi.alt)$), local ($e^(i phi.alt (x))$), gradients ($nabla phi.alt (x)$), gaugable?
  + Amplitude - probability ($| phi.alt (x) |^2$)
+ Many body:

  + Hilbert space complexity
  + Hamiltonian